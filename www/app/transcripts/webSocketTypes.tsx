export type TopicType = {
  timestamp: number;
  title: string;
  transcript: string;
  summary: string;
  id: string;
};

export type TranscriptType = {
  text: string;
};

export type FinalSummaryType = {
  summary: string;
};

export type StatusType = {
  value: string;
};
